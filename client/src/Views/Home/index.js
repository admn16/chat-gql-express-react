import React from 'react'
import { AppBar, Button, Card, Input, SideBar } from '../../Components'

const Home = () => (
  <section>
    <SideBar>
      <AppBar title="Chat GQL" />    
      home
    </SideBar>
  </section>
)

export { Home }